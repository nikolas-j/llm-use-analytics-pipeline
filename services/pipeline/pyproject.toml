[tool.pytest.ini_options]
markers = [
    "integration: marks tests that require AWS credentials and make real API calls (deselect with '-m \"not integration\"')",
]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
